# 每日计划 App · 实现计划

- 日期：2026-07-19
- 依据：`设计文档.md`（已批准）
- 原则：每个阶段有明确产出和验收标准，出问题在当阶段修复后再前进

## 阶段 0：GitHub 准备（唯一需要用户操作的阶段）

| 步骤 | 内容 | 验收 |
|---|---|---|
| 0.1 | 用户执行 `gh auth login` 完成 GitHub 登录（无账号先注册） | `gh auth status` 显示已登录 |
| 0.2 | 创建私有仓库 `dailyplan`，关联本地仓库 | `git remote -v` 正常 |

## 阶段 1：项目脚手架与应用代码

| 步骤 | 内容 |
|---|---|
| 1.1 | Android 项目骨架：`settings.gradle.kts`、根/模块 `build.gradle.kts`、`gradle.properties`、`AndroidManifest.xml`。本地不装 Gradle——CI 里用 `gradle/actions/setup-gradle` 直接提供，仓库不需要 wrapper jar |
| 1.2 | 数据层：`Task`/`Period`/`TodayState`/`DayRecord` 模型 + kotlinx.serialization + DataStore 读写仓库类 |
| 1.3 | 核心逻辑：逻辑日期计算（减 4 小时取日历日）、启动/回前台结算归档（照设计文档 §7） |
| 1.4 | UI：深色主题（设计文档 §5.1 配色）、今天页（三板块、＋弹层、点击划掉、长按删除）、已完成页（今日 + 历史倒序）、底部导航 |
| 1.5 | 单元测试：逻辑日期边界（3:59/4:00、跨月跨年）、结算规则（有/无任务）、序列化往返 |

**版本锁定**（互相兼容的已知稳定组合）：Gradle 8.9 / AGP 8.5.2 / Kotlin 2.0.20（内置 Compose 编译器）/ Compose BOM 2024.09 / DataStore 1.1.1 / kotlinx-serialization 1.7.x / JDK 17 / minSdk 26 / targetSdk 34

## 阶段 2：签名与云端构建

| 步骤 | 内容 |
|---|---|
| 2.1 | 本地用 Git Bash 自带 openssl 生成 PKCS12 签名密钥（RSA 2048，有效期 30 年），**不提交进仓库**（.gitignore 已含 `*.p12` 类目） |
| 2.2 | 密钥 base64 与口令写入仓库 Secrets：`KEYSTORE_B64`、`KEYSTORE_PASSWORD`、`KEY_ALIAS` |
| 2.3 | `release` 构建配置从环境变量读取签名信息；本地无密钥时构建脚本不报错（仅 CI 签名） |
| 2.4 | GitHub Actions workflow：push 到 main 触发 → JDK 17 → Gradle 8.9 → 还原密钥 → `test` → `assembleRelease` → 上传 APK 为构建产物 |

## 阶段 3：首次构建与修复循环

| 步骤 | 内容 | 验收 |
|---|---|---|
| 3.1 | push 触发首次构建，`gh run watch` 跟踪 | — |
| 3.2 | 如失败：读日志 → 修复 → 再 push，循环直到通过（首次云端构建常见 1–3 轮修复，属正常） | Actions 全绿，测试通过 |
| 3.3 | `gh run download` 拉取 APK 到 `E:\claude\每日计划App\发布\` | APK 文件存在，约 5–10 MB |

## 阶段 4：交付与验收

| 步骤 | 内容 |
|---|---|
| 4.1 | 用户用微信/QQ 把 APK 传到手机安装（允许"未知来源"） |
| 4.2 | 真机验收清单：三板块添加计划 → 点击划掉/恢复 → 长按删除 → 已完成页显示 → 改手机日期到明天重开 app 验证归档重置 |
| 4.3 | 写《安装与使用说明.md》（含以后如何让我更新功能的流程） |
| 4.4 | 用户反馈问题 → 修复 → 重新构建交付，循环到满意 |

## 风险与预案

- **CI 构建失败**：版本组合已锁定为已知稳定搭配，仍失败则按日志逐个修复（3.2 循环）
- **国内拉取 GitHub 不稳**：电脑端 `gh` 走 API 通常可用；若超时则重试或换时段
- **openssl 生成的 PKCS12 与 Java 17 兼容性**：生成后在 CI 首次构建即验证；不兼容则退路是在 CI 内用 keytool 一次性生成密钥并回存 Secrets
