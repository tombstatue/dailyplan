# 每日计划 App · 设计文档

- 日期：2026-07-19
- 状态：已与用户逐节确认，待最终审阅
- 项目路径：`E:\claude\每日计划App\`

## 1. 项目概述

一款完全离线的安卓原生每日计划应用。一天分为早晨/下午/晚上三个板块，各板块可添加计划；点击计划划掉表示完成，再点恢复；每天自动重置页面，历史记录（含完成与未完成）永久保留可查。

## 2. 已确认的需求决定

| 决定点 | 结论 |
|---|---|
| 目标平台 | 安卓手机，原生 App（APK） |
| 编译方式 | GitHub Actions 云端编译（本地零环境） |
| 界面风格 | 深色夜间风（紫色强调） |
| 每日重置 | 新一天页面清空，前一天完整归档进历史（含未完成项） |
| 划掉行为 | 任务留在原位显示删除线，再次点击可恢复 |
| 重复任务 | 不需要，每天手动输入 |
| 一天分界线 | 凌晨 4:00（熬夜时段算前一天） |

## 3. 技术方案

- **语言/框架**：Kotlin + Jetpack Compose，单 Activity，两个页签
- **最低版本**：minSdk 26（Android 8.0+），targetSdk 34
- **存储**：Jetpack DataStore (Preferences) + kotlinx.serialization 序列化 JSON；完全本地，**不申请任何网络权限**
- **应用名**：每日计划；包名 `com.tombstatue.dailyplan`（可改）

## 4. 构建与分发

1. 代码托管在用户的 GitHub 私有仓库（免费）
2. GitHub Actions workflow：push 触发 → 运行单元测试 → 编译 release APK → 签名 → 上传为构建产物/Release
3. **签名密钥固定**：密钥库以 base64 存入仓库 Secrets，每次构建用同一密钥签名，保证后续更新可直接覆盖安装、数据不丢
4. APK 获取：优先「电脑上 `gh` 命令行拉取 → 微信/QQ 传手机」；也可手机直接开 Release 页下载
5. 手机首次安装需允许"安装未知来源应用"
6. 前提：用户需有 GitHub 账号并在电脑上完成 `gh auth login`（实现阶段引导）

## 5. 界面设计

### 5.1 配色（深色夜间风，草图已确认）

| 用途 | 色值 |
|---|---|
| 页面背景 | `#12121A` |
| 卡片背景 | `#1E1E2A`，边框 `#2A2A3A` |
| 正文 | `#E8E8F0`；次要文字 `#6B6B80`；划掉文字 `#55556A` |
| 主强调（按钮/选中） | `#7C6CFF` |
| 板块色：早/午/晚 | `#FFB86C` / `#8BE9FD` / `#BD93F9` |
| 未完成标记 | 文字 `#E08A8A`，底 `#3A2A2A`；全勤 `#8BE9A0` |

### 5.2 今天页（主页）

- 顶部：日期（如「7月19日 · 周日」）+ 今日进度（已完成/总数）
- 三个板块卡片：🌅 早晨 / ☀️ 下午 / 🌙 晚上，右上角各有 ＋ 按钮
- 点 ＋ → 底部弹出输入框（标明添加到哪个板块），确认后加入该板块列表尾部
- 点击任务 → 切换完成状态（删除线 + 变灰 ↔ 恢复），即时落盘
- 长按任务 → 确认框 → 删除（用于输错场景，与"划掉"独立）
- 底部导航：今天 | 已完成

### 5.3 已完成页

- 顶部：今天已划掉的任务（按板块图标标注）
- 其下按日期倒序列出历史：每天一张卡片，标题为日期 + 完成率（如 3/4），内容为已完成项（删除线）与未完成项（红色「未完成」标记）；全部完成的天显示 🎉
- 历史永久保留，不自动清理（纯文本，体积可忽略）

## 6. 数据模型

```kotlin
enum class Period { MORNING, AFTERNOON, EVENING }

data class Task(
    val id: String,        // UUID
    val text: String,
    val period: Period,
    val done: Boolean,
    val createdAt: Long    // epoch millis
)

data class TodayState(
    val logicalDate: String,   // "2026-07-19"，逻辑日期
    val tasks: List<Task>
)

data class DayRecord(
    val date: String,          // 归档日的逻辑日期
    val tasks: List<Task>      // 含完成与未完成，快照
)
// 存储：TodayState 与 List<DayRecord> 各占一个 DataStore key，JSON 序列化
```

## 7. 每日重置逻辑（核心）

- **逻辑日期** = 当前时间减 4 小时后所在的日历日期（实现 4:00 分界）
- 触发时机：App 启动及每次回到前台（`ON_RESUME`）时检查
- 结算规则：若 `TodayState.logicalDate` ≠ 当前逻辑日期：
  1. 若前一状态**有任务**，整体作为 `DayRecord` 追加进历史（无任务的天不产生历史记录）
  2. 清空今天页，`logicalDate` 更新为当前逻辑日期
- 不依赖任何后台服务/定时器，不受安卓省电策略影响
- 用户手动修改系统日期：按新日期正常结算，不做特殊处理

## 8. 边界情况与错误处理

- 空内容或纯空格：不允许添加
- 长文本：自动换行完整显示，不截断
- 每次增/删/划掉立即写入 DataStore，进程被杀不丢数据
- 板块内任务数量不设上限，页面整体可滚动
- 空状态：今天页板块无任务时显示浅色提示文字；已完成页无记录时显示「还没有完成的计划」提示
- 跨天瞬间正在使用：下一次回前台/启动时才结算（可接受的简化）

## 9. 测试策略

- 单元测试覆盖：逻辑日期计算（含 4:00 边界、跨月/跨年）、结算归档规则（有/无任务、状态快照）、序列化往返
- CI 中测试先于打包运行，测试不过不出 APK
- UI 层：编译期检查 + 真机试用反馈迭代

## 10. 已知限制（已向用户说明）

- 数据仅存手机本地：卸载 App 或换手机会丢失全部记录，无云同步
- 国内直连 GitHub 下载 APK 可能慢，以电脑中转传输为主

## 11. 明确不做（本期范围外）

- 每日重复任务、提醒/通知、云同步、数据导出、多端同步、iOS 版本
