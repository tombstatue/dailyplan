# 每日计划 App · 番茄钟设计文档（v1.3）

- 日期：2026-07-21
- 状态：已与用户确认（任务绑定+独立模式 · 自定周期 · 后台+通知 · 今日汇总）
- 前置：v1.2 批量重复日程

## 1. 功能概述

新增番茄钟：底部第四个标签「🍅 番茄」，圆形进度倒计时、工作/短休/长休三模式、后台运行通知、可选绑定今天计划任务、今日专注汇总。

## 2. 设计决定

| 决定点 | 结论 |
|---|---|
| 入口 | 底部导航新增第四个标签「🍅 番茄」 |
| 任务联动 | 可选绑定：长按今天页任务 → "🍅 开始专注"；未绑定时独立使用 |
| 后台运行 | Foreground Service 常驻通知，锁屏/切走继续计时 |
| 时长定制 | 长按模式按钮弹出步进滑块（1-120 分，5 分钟步进），仅本次有效 |
| 今日汇总 | 主页顶部实时累计：专注时长 + 完成次数；每天重置 |
| 通知方式 | 结束时推送通知 + 短暂震动 |

## 3. 界面规格

### 番茄页
- 顶部：`🍅 1h 40m` 今日总专注时长 + `4 次` 完成次数
- 任务标签条（可选）：显示绑定的任务内容 + 已完成番茄数 + ✕ 解绑
- 圆形进度环：剩余比例弧线，中央大字倒计时 + 当前模式名
- 三模式切换按钮：工作 25（红）/ 短休 5 / 长休 15；长按弹出调整
- 暂停/开始 + 重置按钮
- 未绑定时底部显示「📋 选择今天的计划开始专注…」入口

### 从任务启动
- 今天页长按未完成任务 → 额外出现「🍅 开始专注」选项 → 选时长 → 自动跳转番茄页并绑定

## 4. 架构

```
PomodoroEngine（单例，持有 StateFlow<TimerState>）
    ├── PomodoroService（Foreground Service，用 Engine 驱动通知更新）
    └── PomodoroViewModel（UI 层使用，管理设置 + 今日计数 + 任务绑定）
```

- `TimerState`：mode / remainingSec / totalSec / running / boundTaskId
- Engine 用 `delay(1000)` 循环驱动倒数
- Service 仅在 running=true 时启动前台通知，running=false 时 stopForeground/stopSelf

## 5. 不做

- 历史记录持久化（每日重置）、周/月报、自定义音效、休息自动开始
