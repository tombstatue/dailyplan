# 每日计划 App · 日历功能实现计划（v1.1）

- 依据：`设计文档-日历功能v1.1.md`（已批准）
- 构建/推送/签名流程复用 v1，全程无需用户操作

## 步骤

| # | 内容 | 涉及文件 |
|---|---|---|
| 1 | 数据模型：新增 Event/DayPlan；Task+fromPlan、TodayState/DayRecord+events（全部带默认值保兼容） | `data/Models.kt` |
| 2 | 结算逻辑 v2：`RolloverResult`，含跳过日逐日归档、规划并入、plans 清理 | `logic/DayLogic.kt` |
| 3 | 存储层：`future_plans` key、结算事务改造、未来规划增删（upsert，空条目自动清除） | `data/PlanRepository.kt` |
| 4 | ViewModel：暴露 plans、未来规划操作函数 | `ui/PlanViewModel.kt` |
| 5 | 共享组件抽取：输入弹层、删除确认框（今天页与日详情页共用） | `ui/Components.kt`（新） |
| 6 | 今天页：事件加粗横幅、fromPlan 任务加粗（置顶由数据顺序保证） | `ui/TodayScreen.kt` |
| 7 | 日历页：自绘月网格（42 格、翻月、今天高亮、事件文字条）、点击分流（今天→切页签） | `ui/CalendarScreen.kt`（新） |
| 8 | 日详情页：未来可编辑（事件+三时段，无打勾）/ 过去只读（未完成上、已完成下） | `ui/DayDetailScreen.kt`（新） |
| 9 | 主界面：底部导航加"日历"页签 | `MainActivity.kt` |
| 10 | 版本号 1.1 | `app/build.gradle.kts` |
| 11 | 测试：结算 v2 全场景、新旧 JSON 兼容、DayPlan 往返 | `DayLogicTest.kt` |
| 12 | 提交 → api-push → 云端构建 → 拉取 APK → 更新使用说明 | — |

## 验收

- CI 测试全绿、APK 覆盖安装后 v1 数据完整保留
- 真机：日历添加未来事件/日程 → 改日期到那天 → 自动置顶加粗出现
