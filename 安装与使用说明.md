# 每日计划 App · 安装与使用说明

## 一、装到手机（首次）

1. 把 `发布\app-release.apk` 传到手机：
   - **推荐用 QQ**（微信会拦截 .apk 文件；QQ 的"我的电脑"传输没问题）
   - 或者数据线复制
2. 在手机上点开 APK 安装。系统会提示"不允许安装未知应用"→ 点**设置**→ 允许该来源 → 返回继续安装
3. 桌面出现紫底白勾图标"每日计划"

## 二、功能速查

| 操作 | 效果 |
|---|---|
| 点板块右上角 **＋** | 给 🌅早晨 / ☀️下午 / 🌙晚上 添加计划 |
| **点击**一条计划 | 划掉（完成）；再点一次恢复 |
| **长按**一条计划 | 删除（用于输错的情况，会弹确认框） |
| 底部「已完成」页 | 上方是今天已完成的；下方按日期倒序列出历史，含未完成标记 |

## 三、每日重置规则

- **凌晨 4:00** 为一天的分界：熬夜到凌晨 1、2 点时，添加/划掉都还算"今晚"
- 过了 4 点后**下次打开 app** 时自动结算：昨天的计划（连同完成情况）进入历史，今天页清空
- 历史永久保留

## 四、数据须知

- 所有数据只存在手机本地，**不联网、不上传**
- **卸载 App 或换手机会丢失全部记录**（更新版本用覆盖安装，不会丢）

## 五、以后想改功能怎么办

1. 打开 Claude Code，描述想改什么（项目在 `E:\claude\每日计划App`）
2. Claude 改完代码后运行 `node --use-system-ca tools/api-push.mjs` 推送，云端自动出新 APK
3. 下载新 APK 直接覆盖安装即可，数据不丢

## 六、两个不能动的东西

- `E:\claude\每日计划App\签名密钥\` 文件夹：**删了以后就无法覆盖安装更新**（必须卸载重装、数据清零）
- GitHub 仓库 `tombstatue/dailyplan` 的三个 Secrets：删了云端就打不出签名包
