# 更新日志

每日计划 App 的版本迭代记录。版本规则：功能更新 +0.1，修补不改号（以 GitHub Release 为准）。

## v1.4 · 2026-09-08

### 新增
- 🎨 **主题系统**：四套主题（夜间紫/浅色清新/冷蓝静谧/自定义图片）；自定义主题上传图片作为全局背景，文字颜色按图片亮度自动深浅适配（布局不变，只换色）
- 🕊️ **白名单**：专注页一键打开白名单应用；试用其他应用会被自动拉回专注页；应用列表勾选管理（需授权使用情况访问+悬浮窗）
- ⚙️ **设置页**（底部新标签「⚙️ 设置」）：主题、白名单管理、专注验证码开关
- 🖼️ **专注页背景**：可单独上传图片做背景（不设置则跟随全局主题）

### 修复
- 🍅 **番茄钟计时重构（墙钟制）**：息屏、杀进程后重开，倒计时按真实时间恢复，不再从头走——原「息屏不继续计时」「停止后不能续计」修复
- **暂停无缝续计**：暂停后剩余时间持久化，再开始原样续上
- **计时结束兜底**：精确闹钟（进程被杀也能收到结束通知+震动）
- 专注验证码改为设置开关（默认关闭，退出一键确认）

### 技术
- `TimerLogic` 纯函数化 + 新增 12 个单元测试（CI 自动执行）
- 白名单/主题/设置全部 DataStore 持久化
- 包可见性声明（queries）：白名单应用列表可显示全部桌面应用

## v1.3 · 2026-07-21

### 新增
- 🍅 **番茄钟**（底部新标签「🍅 番茄」）：工作/短休/长休三模式、圆形进度倒计时
- **后台运行**：锁屏或切走 App 后计时继续，通知栏常驻显示剩余时间；结束时推送提醒+震动
- **任务联动**：长按今天页的未完成任务 →「🍅 开始专注」→ 定时并关联任务
- **独立模式**：不绑定任务直接使用；可从番茄页「📋 选择今天的计划」后绑定
- **自定义时长**：长按任意模式按钮弹出滑块（1–120 分钟），本次有效
- **今日汇总**：主页顶部实时累计今日专注时长和完成次数

## v1.2 · 2026-07-19

### 新增
- 📦 **批量规划**（日历页顶部）：支持两种模式
  - **日期范围**：选择起止日期，将同一条日程批量添加到范围中的每一天
  - **每周重复**：选择起止日期 + 勾选星期（如周一三五），将日程添加至匹配的日期
- 添加后即显示撤销提示（SnackBar + 撤销按钮），批量错误可一键回滚
- 冲突处理：若目标日期已有完全相同（文字+时段）的日程则跳过不重复添加

## v1.1 · 2026-07-19

### 新增
- 📅 **日历页**（底部新页签）：月视图、左右翻月、今天紫色高亮
- **重要事件**：可给任意未来日期添加（如考试、截止日期），以红色文字条显示在日历格上
- **日详情页**：点未来日期可提前规划早/中/晚日程（长按删除，未来日程不能打勾）；点过去日期只读回看，未完成在上、已完成在下；点今天跳回「今天」页
- **规划自动并入**：提前写的日程/事件到当天自动出现在「今天」页——事件顶部加粗横幅，日程置顶加粗，与临时添加的区分
- **跳过日归档**：几天没打开 app，被跳过日期的规划以"全部未完成"记入历史，不会丢失

### 技术
- 数据结构向后兼容升级（新字段全部带默认值），从 v1.0 覆盖安装数据原样保留
- 跨天结算逻辑重写为纯函数，新增 7 个单元测试（含多天跳过、新旧格式互读）

## v1.0 · 2026-07-19

首个版本。

- 一天分 🌅早晨 / ☀️下午 / 🌙晚上 三个板块，各自添加计划
- 点击计划划掉（完成），再点恢复；长按删除
- 「已完成」页：今日完成项 + 按日期倒序的完整历史（含未完成标记与完成率）
- 每日自动重置：以凌晨 4:00 为一天分界，打开 app 时结算归档，不依赖后台
- 完全离线：数据仅存手机本地，无任何网络权限
