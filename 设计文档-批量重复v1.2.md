# 每日计划 App · 批量重复日程设计文档（v1.2）

- 日期：2026-07-19
- 状态：已与用户确认（方案B·日历页独立入口 · 追加跳过相同 · stamp方式）
- 前置：v1.1 日历规划

## 1. 功能概述

新增「批量规划」：从日历页顶部入口，一次输入可将同一条日程批量添加到多天——支持两种模式：日期范围（每天）、每周重复（指定星期几）。

## 2. 设计决定

| 决定点 | 结论 |
|---|---|
| 底层方式 | 批量生成独立 DayPlan 条目（stamp），不做规则引擎 |
| UI 入口 | 日历页顶部「📦 批量规划」按钮 → 底部弹层 |
| 模式切换 | 弹层内两个标签：日期范围 / 每周重复 |
| 冲突处理 | 追加；若该日期已有完全相同（text+period）的日程则跳过 |
| 批量内容 | 仅日程（早/中/晚），不含事件（事件天然绑定具体日期） |
| 撤销机制 | 批量操作后显示"撤销"按钮 3 秒（batchId 标记追踪），超时消失 |

## 3. 界面规格

### 3.1 日历页（改动）

- 标题栏与月份之间新增一行：`📦 批量规划` 按钮（Accent 文字）

### 3.2 批量规划面板（BottomSheet，新）

两个标签切换：

**「日期范围」标签**
- 开始日期选择器 / 结束日期选择器 / 时段下拉 / 日程文字输入
- 底部预览：「将添加至 7月20日 ~ 8月31日 · 共 43 天」

**「每周重复」标签**
- 开始日期 / 结束日期 / 星期多选（一~日，多选 toggle chip）/ 时段 / 日程文字输入
- 底部预览：根据选择动态计算天数

**确认后**：面板关闭 → 短暂显示 SnackBar "已添加至 43 天" + 撤销按钮（3 秒倒计时，到 0 消失或点撤销触发回滚）

## 4. 数据模型

```kotlin
data class Task(..., val batchId: String = "") // 仅批量生成时写入 UUID；撤销用；默认值保兼容
// DayPlan 无变更
```

撤销实现：`batchId` 记录在 PlanRepository 内存中（ViewModel 持有 `undoBatchId: String?`），撤销时 filter 掉所有 batchId 匹配的 entry。

## 5. 边界情况

- 开始日期 < 今天 → 不允许，提示"只能规划未来日期"
- 结束日期 < 开始日期 → 自动对调
- 空文字不允许提交
- 日期范围超过 180 天 → 提示确认（性能考虑，实际限制可调）
- 每周重复模式选了 0 个星期 → 不允许提交

## 6. 不做

- 节假日/周末跳过（需联网）、批量事件、批量编辑/修改已有条目、模板保存复用
