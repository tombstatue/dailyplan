# 每日计划 App · 番茄钟重构 + 白名单 + 主题设计文档（v1.4）

- 日期：2026-09-08
- 状态：已与用户确认（墙钟计时 · 白名单入口+自动拉回 · 四套主题+自定义背景 · 验证码开关 · 设置页签）
- 前置：v1.3 番茄钟（锁屏固定 + 20 位验证码 + delay 计时）

## 1. 功能概述

1. **番茄钟计时核心重构**：墙钟制 + 状态持久化，解决「息屏/杀进程后不继续计时」「暂停后不能接着计」两个问题
2. **白名单**：专注页提供白名单应用的打开入口；检测到使用了非白名单应用时自动拉回专注页
3. **主题系统**：三套预设主题（夜间紫保留 / 浅色清新 / 冷蓝静谧）+ 自定义图片主题（自动适配文字颜色）；专注页可单独自定义背景图
4. **设置页**：新增底部第五页签，集中主题、白名单、验证码开关

## 2. 设计决定

| 决定点 | 结论 |
|---|---|
| 计时基准 | 墙钟制（绝对截止时间戳 `endAtMillis`），剩余 = `endAt - now`；不依赖 delay 节拍准确性 |
| 状态持久化 | DataStore（复用 PlanRepository 的 JSON 模式），每次状态变更写盘；App 启动/服务重启时水合恢复 |
| 服务策略 | `START_STICKY`；完成时刻用 `AlarmManager` 精确闹钟兜底提醒 |
| 专注页形态 | 保留 v1.3 全屏圆环倒计时体验（FocusActivity），不做全屏悬浮窗常驻拦截 |
| 白名单行为 | 专注页图标入口直接启动白名单应用；`UsageStatsManager` 每 2s 轮询前台包名，运行中检出非白名单应用 → 自动拉回专注页 |
| 后台启动豁免 | 申请 `SYSTEM_ALERT_WINDOW`：Android 10+ 允许持此权限的应用后台启动 Activity |
| 验证码 | 20 位验证码改为设置开关，默认关闭（开=退出/拉回场景需输码） |
| 主题接入方式 | `Theme.kt` 顶层色值改为同名 `@Composable get()` 读全局状态；Screens 零改动；FocusActivity 的 3 处硬编码色一并纳入主题 |
| 自定义主题 | `PickVisualMedia` 系统照片选择器（无需存储权限）→ 图片存 `filesDir`；平均亮度判定文字深/浅色；`palette-ktx` 提取主色做强调色；布局不动 |
| 版本策略 | v1.4 一次发布，两里程碑内测（①计时+白名单 ②主题） |

## 3. 架构

```
PomodoroEngine（单例） ←— DataStore（timer_state JSON，墙钟字段+会话计数）
    ├── PomodoroService（START_STICKY 前台服务；通知含"回到专注"Action）
    ├── FocusActivity v2（计时 UI + 白名单图标入口 + 自定义背景图）
    ├── UsageWatcher（前台应用轮询 → 非白名单 → 拉回 FocusActivity）
    └── PomodoroViewModel（UI 绑定）
ThemeStore（DataStore） → 全局色板（日期重置性状态：themeId/customImagePath/focusImagePath）
SettingsScreen（第五底页签）
```

## 4. 计时核心（重构旧 delay 逻辑）

### 状态字段（TimerState 扩展，全部持久化）

`mode / totalSec / endAtMillis:Long? / remainingSec:Int / running / finished / boundTaskId / boundTaskText / sessionsToday / focusSecToday` 等。验证码开关、白名单列表为**用户偏好设置**（DataStore 独立 key），不属于 TimerState。

### 规则

| 操作 | 行为 |
|---|---|
| 开始/恢复 | `endAt = now + remainingSec*1000`；`running=true`；写盘；tick 仅刷新显示 |
| 暂停 | 由 `endAt - now` 结算 `remainingSec` 落盘；`running=false`；**保留 remainingSec 供恢复** |
| 重置/切模式 | 清 endAt、按新总时长置 remainingSec；写盘 |
| App/服务启动恢复 | 读盘；`running && endAt 已过` → 结清（WORK 计入完整专注时长，标记 finished）；`running && 未过` → 恢复倒计时；非 running → 原样 |
| 到点 | 结清 + finished；`AlarmManager.setExactAndAllowWhileIdle` 兜底发通知/震动 |

### 完成语义

完成一个工作番茄 = `sessionsToday+1`、`focusSecToday += totalSec`（从 v1.3 沿用）；断点恢复的"过期但结束"场景同样按完整番茄计入。

## 5. 专注页 v2 与白名单

### FocusActivity 改动

- 保留：全屏圆环倒计时、锁屏上方显示、`startLockTask` 屏幕固定、完成页
- 新增：**白名单应用图标横排**（PackageManager 取图标+名称，点击 → `stopLockTask` → `startActivity` 启动）；**背景图**（主题图 or 专注页专属图；亮度自适应文字色，同 §6 逻辑）
- 验证码开关：开 → 退出/被拉回后需输码；关 → 一键确认退出

### UsageWatcher（白名单核心）

- `PACKAGE_USAGE_STATS` 特殊授权后，每 2s `UsageEvents` 查询最近 `MOVE_TO_FOREGROUND` 包名
- 计时 `running=true` 且前台包名 ∉ 白名单且 ∉ 本应用 → `startActivity(FocusActivity)`（`SYSTEM_ALERT_WINDOW` 豁免后台启动限制；singleInstance 已配置，会复用实例）
- 白名单应用或本应用 → 不干预
- 已知延迟：事件回读有 1~3s 延迟，属行业通行体验，接受

### 白名单管理

设置页列表展示全部已安装应用（PackageManager），勾选加入/移出；顶部「开启使用情况访问」授权引导。

## 6. 主题系统

### 色板接入

`Theme.kt` 的 `Bg/CardBg/CardBorder/TextMain/TextDim/TextStruck/Accent/UndoneRed/UndoneBg/AllDoneGreen/SheetBg` 改为：

```kotlin
val Bg: Color @Composable get() = ThemeHolder.current.bg
```

- 所有页面 import 名不变（编译期错误仅出现在非 Composable 上下文引用处，全量 grep 修正）
- FocusActivity 硬编码色（`0xFF0D0D15` 背景、`0xFFE94560` 工作红、`0xFF4ECCA3` 休息绿，共 3 处）纳入主题字段或按主题色渲染
- `ThemeHolder` 持有 `StateFlow<ThemeSpec>`；ThemeSpec = 上述 12 色数据类 + 背景图 URI

### 四套主题色值

| | 夜间紫（现有，不变） | 浅色清新（新增） | 冷蓝静谧（新增） | 自定义 |
|---|---|---|---|---|
| 背景 | #12121A | #F6F4EE | #0E1621 | 图片+遮罩 |
| 卡片 | #1E1E2A | #FFFFFF | #17202E | 图片+遮罩 |
| 文字 | #E8E8F0 | #2B2B33 | #D9E4F0 | 按亮度 |
| 次文字 | #6B6B80 | #8E8E9A | #7E93A8 | 按亮度 |
| 强调 | #7C6CFF | #3D7BF4 | #5AA7FF | Palette 主色 |
| 底部弹层 | #22222E | #F0EDE6 | #1C2836 | 图片+遮罩 |

（删除线文字、完成绿、未完成红等沿用各主题派生值，实现时按对比度微调）

### 自定义图片主题

1. `PickVisualMedia` 选图（无需任何存储权限）→ 缩放存 `filesDir/custom_bg.jpg`，路径存 DataStore
2. 背景 = 图片全屏 + 半透明遮罩（scrim，暗图 0.35 / 亮图 0.55 系数）保可读
3. **亮度判定**：Bitmap 下采样 32×32 求平均亮度 L；`L < 128` → 浅色文字系（白字），否则深色文字系（黑字）
4. `palette-ktx` 取 `Vibrant`（拿不到则 `Muted`）像素作强调色
5. 全部颜色随主题重建 UI，**布局零改动**

### 专注页背景

设置页「专注页背景」卡片：使用全局主题图 / 单独传图（存 `filesDir/focus_bg.*`）/ 不启用（保持纯色+主题）

## 7. 设置页（第五底页签）

1. **主题卡片**：4 套色板圆形预览 + 「自定义图片」传图入口
2. **专注页背景卡片**：全局图 / 单独传图 / 纯色
3. **白名单管理卡片**：授权引导（使用情况访问、悬浮窗）+ 应用勾选列表
4. **专注验证码开关**：默认关闭

## 8. 权限与依赖增量

| 新增 | 用途 | 说明 |
|---|---|---|
| `PACKAGE_USAGE_STATS` | 前台应用检测 | 特殊授权，设置页一键跳转 `ACTION_USAGE_ACCESS_SETTINGS` |
| `SYSTEM_ALERT_WINDOW` | 后台启动豁免 | 特殊授权，跳转 `ACTION_MANAGE_OVERLAY_PERMISSION` |
| `USE_EXACT_ALARM` | 完成兜底提醒（Android 12+） | 计时类应用豁免，manifest 声明 |
| `palette-ktx` | 图片主色提取 | 一个依赖 |
| `activity-compose` 升版兼容 | `PickVisualMedia` | 需要 activity 1.7+（当前 1.9.2 满足） |

已有权限（FOREGROUND_SERVICE / SPECIAL_USE / VIBRATE / POST_NOTIFICATIONS）不变。

## 9. 错误处理与边界

| 场景 | 处理 |
|---|---|
| 进程被系统清理后重开 | DataStore 水合；过期结清 / 未过继续；服务 START_STICKY 自动重启 |
| 使用情况访问被用户关闭 | 白名单功能降级为"仅入口不监控"；设置页提示重新授权 |
| 悬浮窗权限被关闭 | 拉回功能失效（后台启动限制）→ 用户仍可通过通知栏「回到专注」Action 手动回 |
| 拉回瞬间用户正输入验证码 | 拉回与退出无冲突：退出动作以用户操作为准，拉回仅在前台应用变化时发生 |
| 相册选择器取消 | 无变更，保留原主题 |
| 图片文件损坏 | 解码失败 → 回退纯色主题并提示 |
| 白名单应用在前台时计时结束 | 后台以通知/震动提醒；回到专注页显示完成页 |

## 10. 测试计划

- **单元测试**（JUnit，沿 `DayLogicTest` 模式）：墙钟剩余计算、恢复水合（过期/未过期）、暂停→恢复、完成结清计数、亮度阈值判定函数（纯函数化）
- **真机 ADB 验证**（手机已 Type-C 连电脑）：
  - 里程碑①：息屏后计时继续 → 强杀进程重开恢复 → 暂停后继续接着计时 → 打开白名单应用 → 检测非白名单应用被拉回 → 验证码开/关两态
  - 里程碑②：三套预设主题切换 → 自定义传图（暗图/亮图各一）自适应 → 专注页背景 → 时段色/强调色全局生效
- 目标机型：用户手机 + Android 13/14（targetSdk 34）

## 11. 不做（YAGNI）

- 悬浮窗常驻全屏拦截（已改为拉回式）
- 白名单按应用粒度"允许时长/次数"统计
- 主题自定义手动微调滑杆（亮度自动判定先上，实测不满意再加）
- 周/月报表、专注历史记录持久化
- 休息模式自动开始

## 12. 发布

v1.4 一次发布。流程沿用：本地 commit → `node tools/api-push.mjs` → `gh run watch/download` → 真机验证 → `CHANGELOG.md` 更新 → `gh release create --target <完整SHA>` + upload APK。
