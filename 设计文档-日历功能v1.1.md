# 每日计划 App · 日历规划功能设计文档（v1.1）

- 日期：2026-07-19
- 状态：已与用户分两轮确认，待最终审阅
- 前置：v1 设计见 `设计文档.md`

## 1. 功能概述

新增"日历"页：月视图日历，可给未来日期定义**重要事件**（显示在日历格上）和**日程计划**（早/中/晚）。到了那天，规划内容自动进入"今天"页；过去的日期可回看当天完成情况。

## 2. 已确认的需求决定

| 决定点 | 结论 |
|---|---|
| 规划与今天页联动 | 到当天自动并入今天页；规划的日程**置顶并加粗**，与临时添加的区分 |
| 重要事件当天显示 | 今天页最顶部（日期下方）加粗横幅 |
| 日历格上显示什么 | 只显示重要事件（B 方案：格内文字条），日程不上格子 |
| 点过去日期 | 只读显示当天归档：**未完成在上、已完成在下** |
| 点今天 | 跳转到"今天"标签页（单一数据入口） |
| 日历实现 | Compose 自绘月网格，不引第三方库 |
| 日详情形态 | 全屏子页面（非底部弹层），左上角返回 |

## 3. 概念模型

| | 重要事件 Event | 日程计划（复用 Task） |
|---|---|---|
| 含义 | 当天的大事（考试、截止） | 那天要做的具体事项 |
| 时段 | 无，属于整天 | 早/中/晚 |
| 日历格 | 文字条（红字 `#E08A8A`，底 `#3A2A2A`，超长截断） | 不显示 |
| 当天呈现 | 今天页顶部加粗横幅 📌 | 并入对应板块，置顶+加粗 |
| 未来操作 | 添加/长按删除 | 添加/长按删除，**不能打勾** |

## 4. 界面设计

### 4.1 日历页（新，底部导航第 2 个标签：今天 | 日历 | 已完成）

- 标题栏：`‹  2026年7月  ›`，箭头翻月（不做滑动手势），月份范围不限
- 星期表头周一起始；非本月日期灰显 `#3A3A4A`
- 今天：紫底 `#7C6CFF` 圆角高亮
- 事件文字条数据来源：未来/今天 → 规划存储与今日状态；过去 → 历史归档记录
- 点击任意格子 → 日详情页（今天则切到"今天"标签）

### 4.2 日详情页（新，全屏子页面，草图已确认）

- 标题：`7月25日 · 周六`；左上返回日历
- **未来日期**：📌 重要事件卡（红边）+ 🌅☀️🌙 三个板块卡；各卡右上 ＋ 添加（复用现有底部输入弹层）；长按删除（弹确认）；无打勾交互；底部提示文字"长按可删除 · 未来的日程不能打勾"
- **过去日期**：只读两组——`未完成 (n)` 红标题在上、`已完成 (n)` 绿标题在下（划线），右上角显示完成率 `2 / 4`；有事件则在最上方显示；无归档记录显示"这一天没有记录"

### 4.3 今天页（改动）

- 日期下方新增事件横幅区：📌 + 事件文字，**加粗**，红色系，可多条
- 板块内排序：`fromPlan` 的任务在前（**加粗**显示），当天临时添加的在后（正常字重）；两类都正常打勾/恢复/长按删除

## 5. 数据模型（兼容性优先）

```kotlin
@Serializable
data class Event(val id: String, val text: String)

@Serializable
data class DayPlan(               // "未来规划"存储单元
    val date: String,             // yyyy-MM-dd
    val events: List<Event> = emptyList(),
    val tasks: List<Task> = emptyList()
)

// 既有类型的扩展 —— 全部带默认值，旧 JSON 读取自动兼容，升级不丢数据
data class Task(..., val fromPlan: Boolean = false)
data class TodayState(..., val events: List<Event> = emptyList())
data class DayRecord(..., val events: List<Event> = emptyList())
```

- DataStore 新增 key `future_plans`：`List<DayPlan>`（只存今天之后的日期；空的 DayPlan 及时清除）
- 归档时事件随 DayRecord 一起存，供过去日期详情与日历过去格子显示

## 6. 跨天结算逻辑 v2（纯函数，重写 DayLogic.rollover）

```kotlin
data class RolloverResult(
    val newToday: TodayState,
    val archived: List<DayRecord>,   // 按日期升序
    val remainingPlans: List<DayPlan>
)
fun rollover(state, plans, nowMillis, zone): RolloverResult
```

结算步骤（`state.logicalDate ≠ 当前逻辑日期` 时）：
1. 归档原今天（任务或事件非空才归档）
2. 介于 (原日期, 新日期) 之间被跳过的每一天：若有规划，按"任务全部未完成"归档
3. 新今天 = 新日期的规划：日程标记 `fromPlan=true`、事件进 `events`；无规划则为空白页
4. `remainingPlans` = 移除所有日期 ≤ 新日期的条目（防御性清理）

## 7. 边界情况

- 空输入/纯空格不允许添加（事件与日程一致）
- 未来日程并入今天后即为普通任务（保留 fromPlan 标记仅作显示）
- 日历上过去日期无归档且无事件 → 详情页显示"这一天没有记录"
- 用户改系统日期：按新逻辑日期正常结算，不特殊处理
- 版本：versionName 1.0 → 1.1；覆盖安装保留全部数据

## 8. 测试计划（新增单元测试）

- 结算 v2：次日有/无规划、跳过多天逐日归档（全部未完成）、消费后 plans 清理、归档升序
- fromPlan 标记与置顶排序规则
- 新旧 JSON 互读：旧格式 Task/TodayState/DayRecord 反序列化默认值正确
- Event/DayPlan 序列化往返

## 9. 明确不做（本期范围外）

- 滑动切月手势、事件提醒/通知、农历与节假日、跨天事件、事件编辑（删了重加即可）
